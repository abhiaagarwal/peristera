use crate::client::FlyMachinesClient;


/// A volume associated with a `FlyApp`.
pub struct FlyVolume {
    client: FlyMachinesClient,
    app_name: String,
    volume_id: String,
}

impl FlyVolume {
    pub async fn snapshots() -> 
}
 
pub struct FlyVolumeSnapshot {

}